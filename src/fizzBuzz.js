function fizzBuzz (arr) {
  var points = new Array(100);
        for (var i = 0; i < 100; i++) {
            points[i] = i + 1;
         if(arr % 3 === 0) {
           console.log (Fizz);
         else if( arr % 5 === 0) {
           console.log (Buzz);
         } else {
           (console.log(FizzBuzz);
         }
         return points;
        }
      }
}
